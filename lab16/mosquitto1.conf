# Broker 1 Configuration (Hub)
# -----------------------------
# Role: Central hub in the Star Topology.
# This broker is PASSIVE. It initiates no bridges itself.
# It receives incoming bridge connections from mosquitto2 and mosquitto3.
# This pattern prevents routing loops while ensuring full data replication.

listener 1883
allow_anonymous true

log_dest stdout
